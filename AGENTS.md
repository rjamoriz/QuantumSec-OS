# AGENTS

Permanent repository instructions for Codex:

- Always run `nix flake check` after changes.
- Prefer small commits with clear messages.
- Do not add secrets, keys, or credentials to this repo.
- Document every security-relevant change in `docs/hardening.md`.
